# 🔧 دليل الإعداد الكامل لقاعدة البيانات

## 📋 محتويات الدليل
1. [إعداد Supabase](#إعداد-supabase)
2. [إنشاء قاعدة البيانات](#إنشاء-قاعدة-البيانات)
3. [تكوين التطبيق](#تكوين-التطبيق)
4. [ربط الواجهة الأمامية](#ربط-الواجهة-الأمامية)
5. [استخدام النظام](#استخدام-النظام)

---

## إعداد Supabase

### الخطوة 1: إنشاء حساب Supabase

1. اذهب إلى [supabase.com](https://supabase.com)
2. اضغط "Sign Up"
3. استخدم بريد Google أو Github

### الخطوة 2: إنشاء مشروع جديد

1. اضغط "New project"
2. اختر الاسم: `dr-fertility-clinic`
3. اختر كلمة مرور قوية
4. اختر المنطقة الأقرب (Middle East)
5. اضغط "Create new project"

### الخطوة 3: الحصول على مفاتيح API

1. في الصفحة الرئيسية للمشروع
2. اذهب إلى "Settings" (أيقونة الترس)
3. اختر "API" من الجانب الأيسر
4. انسخ:
   - **Project URL** → ضعه في `VITE_SUPABASE_URL`
   - **anon public** → ضعه في `VITE_SUPABASE_ANON_KEY`

---

## إنشاء قاعدة البيانات

### الخطوة 1: فتح SQL Editor

1. في لوحة تحكم Supabase
2. اذهب إلى "SQL" (من الجانب الأيسر)
3. اضغط "New Query"

### الخطوة 2: تشغيل ملف Schema

انسخ محتوى `supabase_schema.sql` وألصقه في محرر SQL:

\`\`\`sql
-- ملف supabase_schema.sql
-- هذا الملف موجود في المشروع
\`\`\`

ثم اضغط "Run" (Ctrl+Enter)

### الخطوة 3: إضافة البيانات الأولية

1. اضغط "New Query" مجددًا
2. انسخ محتوى `supabase_seed.sql`:

\`\`\`sql
-- ملف supabase_seed.sql
-- هذا الملف موجود في المشروع
\`\`\`

ثم اضغط "Run"

---

## تكوين التطبيق

### الخطوة 1: تحديث ملف .env

في مجلد المشروع، عدّل `.env`:

\`\`\`env
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key_here
VITE_APP_NAME=Infertility CDSS
VITE_ENABLE_ANALYTICS=true
VITE_ENABLE_AUDIT_LOG=true
\`\`\`

استبدل:
- `your-project` باسم مشروعك
- `your_anon_key_here` بمفتاح API الخاص بك

### الخطوة 2: تثبيت المكتبات

\`\`\`bash
npm install
\`\`\`

---

## ربط الواجهة الأمامية

### المكونات الجديدة المضافة:

#### 1. **LoginPage.jsx** - صفحة تسجيل الدخول
- تسجيل دخول المستخدمين
- إنشاء حسابات جديدة
- تخزين بيانات الملف الشخصي

#### 2. **AdminDashboard.jsx** - لوحة تحكم الإدارة
- عرض الإحصائيات
- إدارة المرضى
- إدارة الأدوية
- إدارة الموظفين
- عرض السجلات (audit logs)

#### 3. **PatientManagement.jsx** - إدارة المرضى
- إضافة مريض جديد
- تعديل بيانات المريض
- حذف مريض
- البحث والتصفية

#### 4. **useAuth.js** - خدمة المصادقة
\`\`\`javascript
const auth = useAuth()

// تسجيل الدخول
await auth.signIn(email, password)

// إنشاء حساب
await auth.signUp(email, password, profileData)

// التحقق من الصلاحيات
if (auth.hasPermission('manage_patients')) { ... }

// التحقق من كونه مسؤول
if (auth.isAdmin) { ... }
\`\`\`

#### 5. **supabase.js** - خدمات البيانات
تتضمن 8 خدمات رئيسية:
- `patientService` - إدارة المرضى
- `diagnosisService` - إدارة التشخيصات
- `prescriptionService` - إدارة الوصفات
- `medicineService` - إدارة الأدوية
- `userService` - إدارة المستخدمين
- `auditService` - تسجيل الأنشطة
- `analyticsService` - الإحصائيات
- `exportService` - تصدير البيانات

---

## استخدام النظام

### للمستخدم العادي (طبيب):

1. **تسجيل دخول أو إنشاء حساب**
   - البريد الإلكتروني + كلمة المرور
   - الاسم والتخصص والعيادة

2. **الوصول إلى معالج التشخيص**
   - 7 خطوات للتشخيص الكامل
   - حفظ البيانات في Supabase تلقائيًا

3. **عرض التقارير والوصفات**
   - التشخيص المضبوط حسب WHO 2021
   - قائمة الأدوية المقترحة

### للمسؤول:

1. **الدخول إلى لوحة التحكم**
   - إحصائيات شاملة
   - إدارة جميع المرضى

2. **إدارة الموظفين**
   - عرض الموظفين
   - تعديل الصلاحيات

3. **عرض السجلات**
   - تسجيل جميع العمليات
   - تتبع من قام بأي عملية

---

## أمثلة الكود

### مثال 1: الحصول على جميع المرضى

\`\`\`javascript
import { patientService } from './lib/supabase'

const { data, error } = await patientService.getAll()
if (!error) {
  console.log('Patients:', data)
}
\`\`\`

### مثال 2: إضافة مريض جديد

\`\`\`javascript
const { data, error } = await patientService.create({
  name: 'فاطمة أحمد',
  age: 28,
  phone: '0501234567',
  email: 'fatima@email.com',
  type: 'primary',
  duration: '2'
})
\`\`\`

### مثال 3: تسجيل نشاط

\`\`\`javascript
import { auditService } from './lib/supabase'
import useAuth from './lib/useAuth'

const auth = useAuth()
await auditService.log(
  'CREATE_DIAGNOSIS',
  'تشخيص جديد: PCOS',
  auth.user?.id
)
\`\`\`

### مثال 4: التحقق من الصلاحيات

\`\`\`javascript
const auth = useAuth()

if (auth.isAdmin) {
  // عرض لوحة التحكم
}

if (auth.hasPermission('manage_medicines')) {
  // يمكنه إدارة الأدوية
}
\`\`\`

---

## استكشاف الأخطاء

### المشكلة: "Failed to connect to Supabase"

**الحل:**
1. تحقق من `VITE_SUPABASE_URL` و `VITE_SUPABASE_ANON_KEY`
2. تأكد أن المفاتيح صحيحة
3. أعد تشغيل التطبيق بـ `npm run dev`

### المشكلة: "Relation 'patients' does not exist"

**الحل:**
1. تأكد من تشغيل `supabase_schema.sql`
2. اذهب إلى SQL Editor في Supabase
3. تحقق من وجود الجداول

### المشكلة: "Permission denied"

**الحل:**
1. اذهب إلى "RLS (Row Level Security)" في Supabase
2. تحقق من سياسات الأمان
3. تأكد من أن المستخدم له صلاحيات القراءة والكتابة

---

## الخطوات التالية

✅ قاعدة البيانات معدة
✅ نظام المصادقة جاهز
✅ لوحة تحكم مكتملة
⏭️ **الآن:** اختبر التطبيق بـ `npm run dev`
⏭️ **ثم:** نشّر على الإنترنت (Vercel/Netlify)

---

## الدعم

للمزيد من المساعدة:
- [توثيق Supabase](https://supabase.com/docs)
- [مشاكل شائعة](./TROUBLESHOOTING.md)
- [ملف المرجع السريع](./QUICK_REFERENCE.md)
